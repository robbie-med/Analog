# Analog OS Manual

A single-file-ish static website (HTML/CSS/JS) that documents the Analog Operating System (AOS).

## Use
1. Put the folder anywhere
2. Open `index.html` in a browser

## Features
- Sidebar TOC with active section highlight
- Search (Ctrl/⌘ K or /)
- Expand/collapse all sections
- Print stylesheet (looks like a real manual)

## Edit
All content is in `index.html`. Styling in `style.css`. Print rules in `print.css`.
